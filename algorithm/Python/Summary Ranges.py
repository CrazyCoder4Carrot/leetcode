class Solution(object):
    def summaryRanges(self, nums):
        """
        :type nums: List[int]
        :rtype: List[str]
        """
        temp = nums[0]
        for x in nums:
        	res.add()